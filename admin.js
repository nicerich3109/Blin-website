const API_BASE=(window.BLIN_API_BASE||'').replace(/\/$/,'');
async function api(path,options={}){const r=await fetch(`${API_BASE}${path}`,{credentials:'include',...options,headers:{'Content-Type':'application/json',...(options.headers||{})}});if(!r.ok)throw new Error(await r.text());return r.status===204?null:r.json()}
async function initAdmin(){const state=document.querySelector('#admin-state'),content=document.querySelector('#admin-content');try{const me=await api('/api/me');if(!me.user){location.href=`${API_BASE}/auth/discord`;return}if(!me.site_admin){state.innerHTML='<h3>Доступ запрещён</h3><p>Этот Discord-аккаунт не является администратором Blin.</p>';return}state.innerHTML=`<h3>Доступ разрешён</h3><p>Администратор: ${String(me.user.global_name||me.user.username||'Discord').replace(/[<>]/g,'')}</p>`;content.style.display='block';const admin=await api('/api/admin');document.querySelector('#api-health').textContent=admin.bot.online?'API и бот работают':'Бот отключён';document.querySelector('#system-info').textContent=`Discord-серверов: ${admin.bot.guild_count} · API v${admin.configuration.api_version}`}catch(e){console.error(e);state.innerHTML='<h3>Не удалось проверить доступ</h3><p>Проверьте подключение к Blin API.</p>'}}
document.querySelector('#logout')?.addEventListener('click',async()=>{try{await api('/api/logout',{method:'POST'})}finally{location.href='index.html'}});document.addEventListener('DOMContentLoaded',initAdmin);
